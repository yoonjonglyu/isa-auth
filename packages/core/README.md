# `core`

> TODO: description

## Usage

```
const core = require('core');

// TODO: DEMONSTRATE API
```

1. 기본적인 auth를 제공 프레임워크나 라이브러리에 구애 받지 않아야하니 웹컴포넌트 부터 지원 타 라이브러리나 프레임워크 어댑터 패턴의 wrapper 제공.
2. 기본적으로 auth 상태를 관리할 수 있는 기능이랑 jwt등 기능들을 옵션적으로 선택해서 auth를 관리 할 수 있어야함.
3. Oauth같은 것도 통합적으로 관리 할 수 있어야함.
4. core단에 어느 정도 영역을 할애할지는 고민중. 일단 대충 큰그림을 그리고 결정해볼생각.